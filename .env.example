# Star AR Map - Environment Variables Template
# Copy this file to .env.local for local development

# Node Environment
NODE_ENV=development

# Next.js Configuration
# NEXT_PUBLIC_ prefix makes variables available to the browser
# Note: Most features use browser APIs and don't need server-side env vars

# Optional: Analytics or monitoring (add if needed)
# NEXT_PUBLIC_ANALYTICS_ID=

# Optional: Custom API endpoints (if you add backend features)
# NEXT_PUBLIC_API_URL=

# Development Notes:
# - No API keys needed for core functionality
# - GPS and sensors use browser APIs (permission-based)
# - All astronomy calculations are client-side
# - HTTPS required for sensor access (Vercel provides this automatically)
